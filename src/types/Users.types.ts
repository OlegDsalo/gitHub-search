export interface UsersTypes {
    login: string;
    avatar_url: string;
    id: string;
}

export interface UserReposType {
    name: string;
    html_url: string;
    forks: string;
    stargazers_count: string;
}